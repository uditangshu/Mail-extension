export const config = {
    
    OPENAI_API_KEY: 'your-openai-api-key',
    
    // Generate a secure random string for encryption (32+ characters recommended)
    ENCRYPTION_KEY: 'your-encryption-key',
    
    // Generate a secure random string for salt (16+ characters recommended)
    SALT: 'your-salt-value'
}; 